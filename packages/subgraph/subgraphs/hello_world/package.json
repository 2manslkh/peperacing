{
  "name": "hello_world",
  "version": "1.0.0",
  "description": "Hello World Subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create:local": "graph create --node http://localhost:8020/ argo/hello_world",
    "remove:local": "graph remove --node http://localhost:8020/ argo/hello_world",
    "deploy:local": "graph create --node http://localhost:8020/ argo/hello_world && mustache ./node_modules/@argo/constants/config/localhost.json template.yaml > subgraph.yaml && graph codegen && graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 argo/hello_world",
    "prepare:local": "mustache ./node_modules/@argo/constants/config/localhost.json template.yaml > subgraph.yaml && graph codegen",
    "test": "graph test"
  },
  "dependencies": {
    "@argo/constants": "workspace:^"
  },
  "devDependencies": {
    "mustache": "^4.2.0",
    "matchstick-as": "^0.5.2"
  }
}